import Layout from '../components/Layout';
import { Typography } from '@mui/material';

export default function Reports() {
  return (
    <Layout>
      <Typography variant="h4">Reports Page (Coming Soon)</Typography>
    </Layout>
  );
}
