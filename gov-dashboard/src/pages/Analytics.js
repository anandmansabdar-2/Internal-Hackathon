import Layout from '../components/Layout';
import { Typography } from '@mui/material';

export default function Analytics() {
  return (
    <Layout>
      <Typography variant="h4">Analytics Page (Coming Soon)</Typography>
    </Layout>
  );
}
